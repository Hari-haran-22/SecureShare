# Run ./scripts/Initialize.ps1 to generate fresh local secrets and a wrapping certificate.
MSSQL_SA_PASSWORD=
GRAFANA_ADMIN_PASSWORD=
DOMAIN=localhost
# CI deployment overrides API_IMAGE with an immutable commit tag.
API_IMAGE=secureshare-api:local
